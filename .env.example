ANTHROPIC_API_KEY=
SAM_GOV_API_KEY=
CRON_SECRET=
DAILY_COST_CAP_USD=2.00
DATABASE_URL=./data/govcontracts.db
ANTHROPIC_MODEL=claude-sonnet-4-6
LOG_LEVEL=info
# HTTP Basic Auth password protecting the dashboard (required in production; set via `fly secrets`).
APP_PASSWORD=
# Where per-call AI run traces are written.
TRACE_DIR=./data/traces
